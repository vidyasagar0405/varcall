from pathlib import Path

from varcall.process.process_class import ProcessConfig
from varcall.process.process_dict import HOME_PROCESSES, MASTER_CONFIG

def set_defaults(process: dict[str, dict[str, ProcessConfig]]) -> Path:
    # tab =
    # tool =
    # ext =
    # cwd = Path().cwd().absolute()
    pass
